01. mkdir foundry-fund-me-f23


02. forge init --force

03.  cd Desktop/Projects.eth/FundME

04. forge test

05. forge install smartcontractkit/chainlink-brownie-contracts@0.6.1 
    
     // above used because when we import the FundMe and PriceConverter code from remix
     // chainlink- brown not found so re-install in the cursor.

06. forge compile/build      